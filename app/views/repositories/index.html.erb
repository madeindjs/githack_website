<p id="notice"><%= notice %></p>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item active" aria-current="page"><%= @title %></li>
  </ol>
</nav>

<h1><%= @title %></h1>

<table class="table">
  <thead>
    <tr>
      <th>Url</th>
      <th>Framework</th>
      <th>Leak</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @repositories.each do |repository| %>
      <tr>
        <td><%= link_to repository.url, repository.url %></td>
        <td><%= repository.framework %></td>
        <td>
          <%= repository.unsafe_leaks.count %>
          to check /
          <%= repository.unchecked_leaks.count %>
          unsafe
        </td>
        <td><%= link_to 'Show', repository %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Repository', new_repository_path, class: 'btn btn-primary' %>